# PHASE9_MISSING.md — 消失機能リスト（№0 対比チェック結果）

## 調査方法
- 分割前モノリシック版（login＋アプリ統合）の最終コミット：**`1422dd1:index.html`（6669行）**
- 分割コミット：**`5b4d435`**（ここで `index.html` が 145行へ縮小、`app.html` を新設）
- 比較対象：上記モノリシック版 vs 現状 `index.html` + `app.html` + `js/app.js`
- 比較項目：`function` 定義、`onclick` 参照先、`id="modal-*"`、`id="page-*"`

## 結論サマリ
- **モーダル（`id="modal-*"`）の消失は 0 件**（すべて現存）。
- 関数レベルの消失は 5 件のみ。うち 2 件は置換済み（機能消失なし）、1 件は意図的削除、2 件が要確認。
- 分割作業自体は機能をおおむね保持できている。

---

## 消失機能リスト

### 🔴 復活候補（要・細谷さん判断）

- **[サイドバー] 企業分析ボタン** （現 `app.html` L356 / mock L1286-1287）
  - `onclick="showScreen('company-analysis')"` を呼ぶが、`showScreen` 関数が未定義、かつ `page-company-analysis` セクションも存在しない。
  - クリックしても何も起きない（**モック時点から実装されていなかった可能性が高い** — mock にも `showScreen` 定義なし）。
  - 対応案：ボタンを削除する／「準備中」表示にする／企業分析画面を新規実装する のいずれか。

- **[臨時商品マスタ] 臨時商品チェック `checkTempProduct()`** （mock L4964, L4983）
  - 臨時商品の確認・本登録への昇格処理。現状の `app.js` に未定義（消失）。
  - 現在の臨時商品画面（`page-temp-products` / `renderTempProductTable`）では当該ボタンが無い。

### 🟡 意図的に削除済み（参考・通常は復活不要）

- **[申込ウィザード/配送先] 郵便番号チェック `checkZipCode()` / `checkDlvZipCode()`** （mock L1952, L5230）
  - コミット `de0251c`（Phase5「郵便番号検索削除」）で意図的に削除。
  - 復活させる場合は外部郵便番号API連携が必要。

### ⚪ 置換済み（機能消失なし・対応不要）

- **`buildSalesADataByKey()`** — `loadOrders()` 内のインライン処理（`window.salesADataByKey` 構築）に置換済み。
- **`openSubscDetail()`** — `openServiceDetail()` に置換済み（デッドコードとして削除済み）。

---

## №11（復活作業）への申し送り
1. まず本ファイルを細谷さんと確認。
2. 「🔴 復活候補」のうち復活対象と優先度を確認。
   - 企業分析：実装する／ボタン削除する のどちらか方針決めが必要。
   - 臨時商品チェック：必要なら `checkTempProduct` を再実装。
3. 確定後、機能ごとに復活＋ commit。完了項目に ✅ を付けて再コミット。
